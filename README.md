# Лабораторна робота 2: Комп'ютерне моделювання

Репозиторій містить програмну реалізацію моделі польоту тіла, кинутого під кутом до горизонту з початкової висоти.

## Запуск проєкту

```bash
# створення та активація віртуального середовища (рекомендується)
python3 -m venv .venv
. .venv/bin/activate

# встановлення залежностей
pip install numpy matplotlib prettytable

# запуск моделі
python lab2_model.py
```

## Блок-схема алгоритму

Нижче наведено блок-схему основного алгоритму обчислення характеристик польоту:

```mermaid
flowchart TD
    A[Початок] --> B[Введення вихідних даних
v0, h0, список кутів α]
    B --> C[Для кожного h0]
    C --> D[Для кожного α]
    D --> E[Переведення α з градусів у радіани]
    E --> F[Обчислення складових швидкості
v0x = v0·cosα
v0y = v0·sinα]
    F --> G[Обчислення часу підйому
t_up = v0y / g]
    G --> H[Обчислення максимальної висоти
Hmax = h0 + v0²·sin²α / (2g)]
    H --> I[Обчислення дискримінанта
d = v0²·sin²α + 2gh0]
    I --> J[Обчислення часу польоту
T = (v0y + √d) / g]
    J --> K[Обчислення дальності польоту
L = v0x · T]
    K --> L[Збереження результатів
(v0, h0, α, T, L, Hmax)]
    L --> M{Є ще α?}
    M -- Так --> D
    M -- Ні --> N{Є ще h0?}
    N -- Так --> C
    N -- Ні --> O[Виведення підсумкової таблиці
та побудова графіків]
    O --> P[Перевірка адекватності моделі
(тестовий приклад)]
    P --> Q[Кінець]
```

GitHub відобразить цю діаграму безпосередньо на сторінці репозиторію.